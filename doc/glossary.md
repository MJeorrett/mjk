# Glossary

## Baud Rate
The baud rate is the rate at which data is transferred over a serial connection. It is measured in bits per second (bps).

## Universal Asynchronous Receiver-Transmitter (UART)
A UART is a hardware device that allows a computer to send and receive data over a serial connection.